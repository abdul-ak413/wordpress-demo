apiVersion: v1
kind: Secret
metadata:
  name: secret-readonly
  namespace: kubernetes-dashboard
  annotations:
    kubernetes.io/service-account.name: sa-readonly
type: kubernetes.io/service-account-token
